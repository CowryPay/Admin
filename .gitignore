node_modules
.next
out
build

# Local env files — the admin key is never committed; it lives only in
# sessionStorage at runtime, and NEXT_PUBLIC_API_URL is set in Vercel.
.env
.env.local
.env*.local

.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
